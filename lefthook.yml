pre-commit:
  parallel: true
  commands:
    lint:
      glob: "*.{ts,tsx}"
      run: pnpm oxlint --fix {staged_files}
      stage_fixed: true
    format:
      glob: "*.{ts,tsx,json,md,yml,yaml}"
      run: pnpm oxfmt --write {staged_files}
      stage_fixed: true
